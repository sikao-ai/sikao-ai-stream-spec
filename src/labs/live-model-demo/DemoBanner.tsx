export function DemoBanner() {
  return (
    <aside className="spec-demo-banner" data-demo-only="true">
      <strong>demo-only</strong>
      <span>直连 xAI、localStorage 会话、假听写、假来源接通。不是契约真值。canonical 页面不消费这里。</span>
      <a href="/">回实验室</a>
    </aside>
  );
}
